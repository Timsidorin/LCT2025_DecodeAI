# Step0: Анализ и разделение отзывов на банковские сущности (УЛУЧШЕННАЯ BERT СЕГМЕНТАЦИЯ)
import pandas as pd
import re
import joblib
from tqdm import tqdm
import logging
import nltk
from rapidfuzz import fuzz
import sys
import os
import numpy as np
from sklearn.cluster import DBSCAN
from sentence_transformers import SentenceTransformer, util
from collections import defaultdict
import torch
import hashlib
import pickle
from functools import lru_cache
from transformers import AutoTokenizer, AutoModelForTokenClassification
import razdel
from natasha import Segmenter, MorphVocab, NewsEmbedding, NewsMorphTagger, Doc
from transformers import pipeline
import re

# +++ ДОБАВЛЯЕМ МНОГОПОТОЧНОСТЬ +++
from concurrent.futures import ThreadPoolExecutor, as_completed
import multiprocessing
from threading import Lock
import time

# Скачиваем необходимые данные для nltk
try:
    nltk.data.find('tokenizers/punkt')
except LookupError:
    nltk.download('punkt')

# Настройка логирования
logging.basicConfig(
    filename='bank_entity_analysis_bert_enhanced.log',
    level=logging.INFO,
    format='%(asctime)s | %(levelname)s | %(message)s'
)

# Глобальная блокировка для потокобезопасности BERT модели
bert_lock = Lock()

# +++ КЭШИРОВАНИЕ BERT ЭМБЕДДИНГОВ +++
bert_embedding_cache = {}
CACHE_FILE = "bert_embedding_cache.pkl"


class ProfessionalSegmenter:
    def __init__(self):
        try:
            # Инициализируем Natasha
            from natasha import Segmenter, MorphVocab, NewsEmbedding, NewsMorphTagger, Doc
            self.segmenter = Segmenter()
            self.morph_vocab = MorphVocab()
            self.emb = NewsEmbedding()
            self.morph_tagger = NewsMorphTagger(self.emb)
            self.natasha_available = True
        except ImportError:
            print_warning("Natasha не установлена, используем упрощенную сегментацию")
            self.natasha_available = False

        # BERT для семантической сегментации
        try:
            from transformers import pipeline
            self.bert_classifier = pipeline(
                "text-classification",
                model="cointegrated/rubert-tiny2",
                tokenizer="cointegrated/rubert-tiny2"
            )
            self.bert_available = True
        except:
            print_warning("BERT классификатор недоступен")
            self.bert_classifier = None
            self.bert_available = False

    # В начале файла step0_entity_analysis_bert.py, в классе ProfessionalSegmenter
    def razdel_segmentation(self, text):
        """Точная сегментация на предложения с razdel"""
        if text is None:
            return []
        try:
            import razdel
            return [sentence.text for sentence in razdel.sentenize(text)]
        except ImportError:
            # Fallback на простую сегментацию
            print_warning("Razdel не установлен, используем упрощенную сегментацию")
            import re
            return re.split(r'(?<=[.!?])\s+', text)
        except Exception as e:
            print_warning(f"Ошибка razdel сегментации: {e}, используем упрощенную сегментацию")
            import re
            return re.split(r'(?<=[.!?])\s+', text)


    def natasha_analysis(self, text):
        """Глубокий лингвистический анализ с Natasha"""
        if not self.natasha_available or text is None:
            return None
        try:
            from natasha import Doc
            doc = Doc(text)
            doc.segment(self.segmenter)
            doc.tag_morph(self.morph_tagger)
            return doc
        except:
            return None

    def extract_entities(self, text):
        """Извлекает именованные сущности"""
        if text is None:
            return set()

        doc = self.natasha_analysis(text)
        entities = set()

        if doc and hasattr(doc, 'tokens'):
            for token in doc.tokens:
                if hasattr(token, 'pos') and token.pos in ['NOUN', 'PROPN']:
                    entities.add(token.text.lower())

        return entities

    def detect_products(self, text):
        """Обнаруживает банковские продукты в тексте"""
        if text is None:
            return set()

        products_found = set()
        text_lower = text.lower()

        for product, keywords in PRODUCT_ENTITIES.items():
            for keyword in keywords:
                if re.search(r'\b' + re.escape(keyword) + r'\b', text_lower):
                    products_found.add(product)
                    break

        return products_found

    def are_products_compatible(self, products1, products2):
        """Определяет, можно ли объединять тексты про разные продукты"""
        if not products1 or not products2:
            return True

        # Группы совместимых продуктов
        compatible_groups = [
            {'дебетовые карты', 'кредитные карты', 'премиальные карты', 'зарплатные карты'},
            {'вклады', 'накопительные счета', 'инвестиции'},
            {'кредиты', 'ипотека', 'автокредитование', 'рефинансирование кредитов'},
            {'мобильная связь', 'мобильное приложение', 'интернет-банк'},
            {'бонусные программы', 'страховые и сервисные продукты'}
        ]

        for group in compatible_groups:
            if products1.intersection(group) and products2.intersection(group):
                return True

        return False

    def hybrid_segmentation(self, text):
        """Гибридная сегментация: правила + семантика"""
        if text is None or len(text.strip()) == 0:
            return [text]

        # Сначала разбиваем на предложения
        sentences = self.razdel_segmentation(text)

        if len(sentences) <= 1:
            return [text]

        # Определяем точки разрыва по правилам
        break_points = self.find_break_points(sentences)

        # Создаем сегменты
        segments = []
        start_idx = 0

        for break_idx in break_points:
            segment_sentences = sentences[start_idx:break_idx]
            if segment_sentences:
                segments.append(' '.join(segment_sentences))
            start_idx = break_idx

        # Добавляем последний сегмент
        if start_idx < len(sentences):
            segments.append(' '.join(sentences[start_idx:]))

        return segments if segments else [text]

    def find_break_points(self, sentences):
        """Находит точки разрыва между сегментами"""
        break_points = []

        for i in range(1, len(sentences)):
            current_sent = sentences[i - 1]
            next_sent = sentences[i]

            # Признаки разрыва темы
            if self.is_topic_break(current_sent, next_sent):
                break_points.append(i)

        return break_points

    def is_topic_break(self, sent1, sent2):
        """Определяет, есть ли смена темы между предложениями"""
        if sent1 is None or sent2 is None:
            return False

        # Смена типа продукта
        products1 = self.detect_products(sent1)
        products2 = self.detect_products(sent2)

        if products1 and products2 and not self.are_products_compatible(products1, products2):
            return True

        # Временной разрыв в повествовании
        time_indicators = ['потом', 'затем', 'далее', 'после этого', 'на следующий день']
        if any(indicator in sent2.lower() for indicator in time_indicators):
            return True

        # Резкая смена контекста (обслуживание -> продукт)
        service_indicators = ['обслуживание', 'сервис', 'менеджер', 'сотрудник', 'ответил']
        sent1_has_service = any(indicator in sent1.lower() for indicator in service_indicators)
        sent2_has_service = any(indicator in sent2.lower() for indicator in service_indicators)

        products1 = self.detect_products(sent1)
        products2 = self.detect_products(sent2)

        # Если в одном предложении обслуживание, а в другом продукт - разделяем
        if (sent1_has_service and not sent2_has_service and products2) or \
                (sent2_has_service and not sent1_has_service and products1):
            return True

        return False


# 🔥 ОСНОВНАЯ ФУНКЦИЯ СЕГМЕНТАЦИИ
def professional_bert_segmentation(review_text, segmenter, verbose=False):
    """
    ПРОФЕССИОНАЛЬНАЯ сегментация с точным определением сущностей
    """
    if review_text is None or len(review_text.strip()) == 0:
        return []

    if verbose:
        print(f"📝 Оригинальный текст: {review_text[:100]}...")

    # Используем профессиональный сегментатор
    segments = segmenter.hybrid_segmentation(review_text)

    if verbose:
        print(f"🎯 Получено сегментов: {len(segments)}")

    # Обрабатываем каждый сегмент
    result_segments = []

    for i, segment_text in enumerate(segments):
        if not segment_text or len(segment_text.strip()) == 0:
            continue

        # Определяем сущности в сегменте
        entities = detect_entities_with_bert(segment_text, None)

        if not entities:
            # Если сущностей не найдено, используем общую категорию
            entities = [{
                'entity_type': 'общее_обслуживание',
                'combined_name': 'общее_обслуживание',
                'entity_category': 'general_service',
                'has_service_context': contains_service_context(segment_text),
                'confidence': 'medium',
                'detection_method': 'professional_segmenter'
            }]

        for entity_info in entities:
            result_segments.append({
                'text': segment_text,
                'entity_type': entity_info['entity_type'],
                'service_channel': None,
                'combined_entity_name': entity_info['combined_name'],
                'entity_category': entity_info['entity_category'],
                'is_entity_specific': entity_info['entity_type'] in PRODUCT_ENTITIES,
                'has_service_context': entity_info['has_service_context'],
                'confidence': entity_info['confidence'],
                'segmentation_method': 'professional_hybrid',
                'semantic_coherence': 1.0,
                'detection_method': entity_info.get('detection_method', 'professional')
            })

    return result_segments if result_segments else [{
        'text': review_text,
        'entity_type': 'общее_обслуживание',
        'service_channel': None,
        'combined_entity_name': 'общее_обслуживание',
        'entity_category': 'general_service',
        'is_entity_specific': False,
        'has_service_context': contains_service_context(review_text),
        'confidence': 'medium',
        'segmentation_method': 'professional_fallback',
        'semantic_coherence': 1.0,
        'detection_method': 'fallback'
    }]

def load_embedding_cache():
    """Загружает кэш эмбеддингов из файла"""
    global bert_embedding_cache
    try:
        if os.path.exists(CACHE_FILE):
            with open(CACHE_FILE, 'rb') as f:
                loaded_cache = pickle.load(f)
                for key, value in loaded_cache.items():
                    if isinstance(value, torch.Tensor):
                        bert_embedding_cache[key] = value.cpu()
            print(f"✅ Загружен кэш BERT эмбеддингов: {len(bert_embedding_cache)} записей")
    except Exception as e:
        print(f"⚠️ Ошибка загрузки кэша: {e}")
        bert_embedding_cache = {}


def save_embedding_cache():
    """Сохраняет кэш эмбеддингов в файл"""
    try:
        if len(bert_embedding_cache) > 10000:
            keys_to_keep = list(bert_embedding_cache.keys())[-5000:]
            bert_embedding_cache.clear()
            for k in keys_to_keep:
                if k in bert_embedding_cache:
                    bert_embedding_cache[k] = bert_embedding_cache[k]

        with open(CACHE_FILE, 'wb') as f:
            pickle.dump(bert_embedding_cache, f)
    except Exception as e:
        print(f"⚠️ Ошибка сохранения кэша: {e}")


def get_text_hash(text):
    """Создает хэш текста для использования как ключ кэша"""
    return hashlib.md5(text.strip().encode('utf-8')).hexdigest()


# Загружаем кэш при импорте модуля
load_embedding_cache()

# Сохраняем кэш при завершении
import atexit

atexit.register(save_embedding_cache)


def print_step(message):
    print(f"\n\033[1;36m>>> {message}\033[0m")


def print_success(message):
    print(f"\033[1;32m✓ {message}\033[0m")


def print_warning(message):
    print(f"\033[1;33m⚠ {message}\033[0m")


def print_error(message):
    print(f"\033[1;31m✗ {message}\033[0m")


def print_info(message):
    print(f"\033[1;34mℹ {message}\033[0m")


# === БАНКОВСКИЕ СУЩНОСТИ ===
PRODUCT_ENTITIES = {
    'бонусные программы': [
        'бонусные программы', 'бонусная программа', 'бонусных программ', 'бонусной программы',
        'бонусным программам', 'бонусными программами', 'бонусных программах',
        '35%', 'кешбэк', 'кэшбек', 'акция приведи друга', 'газпром бонус', 'бонусы', 'бонус',
        'получать бонусы', 'получаю бонусы', 'получаешь бонусы', 'получает бонусы',
        'получаем бонусы', 'получаете бонусы', 'получают бонусы', 'получить бонусы',
        'получил бонусы', 'получила бонусы', 'получили бонусы', 'начислять бонусы',
        'начисляю бонусы', 'начисляешь бонусы', 'начисляет бонусы', 'начислить бонусы',
        'начислил бонусы', 'начислила бонусы', 'начислили бонусы', 'бонусная система',
        'программа лояльности', 'программы лояльности', 'лояльность', 'вознаграждение',
        'вознаграждения', 'вознаграждению', 'вознаграждением', 'вознаграждении'
    ],

    'дебетовые карты': [
        'дебетовая карта', 'дебетовой карты', 'дебетовую карту', 'дебетовой картой',
        'дебетовые карты', 'дебетовых карт', 'дебетовым картам', 'дебетовыми картами',
        'карта мир', 'карты мир', 'карту мир', 'картой мир', 'карте мир',
        'карта газа', 'карты газа', 'карту газа', 'картой газа', 'карте газа',
        'карта гпб', 'карты гпб', 'карту гпб', 'картой гпб', 'карте гпб',
        'дебетка', 'дебетки', 'дебетку', 'дебеткой', 'дебетке',
        'оформить дебетовую карту', 'оформил дебетовую карту', 'оформила дебетовую карту',
        'заказать карту', 'заказал карту', 'заказала карту', 'получить карту', 'получил карту',
        'получила карту', 'использовать карту', 'использую карту', 'используешь карту',
        'использует карту', 'платить картой', 'плачу картой', 'платишь картой', 'платит картой',
        'расчетная карта', 'платежная карта', 'банковская карта', 'пластиковая карта', 'карта банка'
    ],

    'накопительные счета': [
        'накопительный счет', 'накопительного счета', 'накопительному счету',
        'накопительным счетом', 'накопительном счете', 'накопительные счета',
        'накопительных счетов', 'накопительным счетам', 'накопительными счетами',
        'накопительных счетах', 'нс', 'ежедневный процент', 'ставка на остаток',
        'накопления', 'накоплений', 'накоплению', 'накоплениями',
        'открыть накопительный счет', 'открыл накопительный счет', 'открыла накопительный счет',
        'пополнять счет', 'пополняю счет', 'пополняешь счет', 'пополняет счет',
        'пополнить счет', 'пополнил счет', 'пополнила счет', 'снимать со счета',
        'снимаю со счета', 'снимаешь со счета', 'снимает со счета', 'накоплять деньги',
        'накопляю деньги', 'накопляешь деньги', 'накопляет деньги', 'копить деньги',
        'коплю деньги', 'копишь деньги', 'копит деньги', 'накопить деньги',
        'накопил деньги', 'накопила деньги', 'проценты на остаток', 'начисление процентов'
    ],

    'кредитные карты': [
        'кредитная карта', 'кредитной карты', 'кредитную карту', 'кредитной картой',
        'кредитные карты', 'кредитных карт', 'кредитным картам', 'кредитными картами',
        'кредитка', 'кредитки', 'кредитку', 'кредиткой', 'кредитке',
        'карта с кредитным лимитом', 'карты с кредитным лимитом', 'карту с кредитным лимитом',
        'кредитный лимит', 'кредитного лимита', 'кредитному лимиту', 'кредитным лимитом',
        'оформить кредитную карту', 'оформил кредитную карту', 'оформила кредитную карту',
        'пользоваться кредиткой', 'пользуюсь кредиткой', 'пользуешься кредиткой',
        'пользуется кредиткой', 'тратить с кредитки', 'трачу с кредитки', 'тратишь с кредитки',
        'тратит с кредитки', 'погашать кредитку', 'погашаю кредитку', 'погашаешь кредитку',
        'погашает кредитку', 'кредитный лимит', 'лимит по карте', 'задолженность по карте'
    ],

    'кредиты': [
        'кредит', 'кредита', 'кредиту', 'кредитом', 'кредите', 'кредиты', 'кредитов',
        'кредитам', 'кредитами', 'кредитах', 'заем', 'заема', 'заему', 'заемом', 'заеме',
        'займ', 'займа', 'займу', 'займом', 'займе', 'ссуда', 'ссуды', 'ссуду', 'ссудой',
        'ссуде', 'ссуд', 'ссудам', 'ссудами', 'ссудах', 'кредитование', 'кредитования',
        'кредитованию', 'кредитованием', 'кредитовании',
        'взять кредит', 'взял кредит', 'взяла кредит', 'брать кредит', 'беру кредит',
        'берешь кредит', 'берет кредит', 'оформить кредит', 'оформил кредит', 'оформила кредит',
        'погашать кредит', 'погашаю кредит', 'погашаешь кредит', 'погашает кредит',
        'выдавать кредит', 'выдаю кредит', 'выдаешь кредит', 'выдает кредит', 'выдать кредит',
        'выдал кредит', 'выдала кредит', 'одобрить кредит', 'одобрил кредит', 'одобрила кредит',
        'отказать в кредите', 'отказал в кредите', 'отказала в кредите', 'процентная ставка',
        'ставка по кредиту', 'условия кредита', 'кредитный договор'
    ],

    'мобильная связь': [
        'мобильная связь', 'мобильной связи', 'мобильную связь', 'мобильной связью',
        'сим карта', 'сим карты', 'сим карту', 'сим картой', 'сим карте',
        'сим-карта', 'сим-карты', 'сим-карту', 'сим-картой', 'сим-карте',
        'газпром мобайл', 'тариф связи', 'мобильный тариф', 'мобильного тарифа',
        'мобильному тарифу', 'мобильным тарифом',
        'подключить сим-карту', 'подключил сим-карту', 'подключила сим-карту',
        'пользоваться связью', 'пользуюсь связью', 'пользуешься связью', 'пользуется связью',
        'сменить тариф', 'сменил тариф', 'сменила тариф', 'подключить тариф',
        'подключил тариф', 'подключила тариф', 'мобильный оператор', 'оператор связи'
    ],

    'мобильное приложение': [
        'мобильное приложение', 'мобильного приложения', 'мобильному приложению',
        'мобильным приложением', 'мобильном приложении', 'мобильные приложения',
        'мобильных приложений', 'мобильным приложениям', 'мобильными приложениями',
        'приложение банка', 'приложения банка', 'приложению банка', 'приложением банка',
        'приложение гпб', 'приложения гпб', 'приложению гпб', 'приложением гпб',
        'мобильный банк', 'мобильного банка', 'мобильному банку', 'мобильным банком',
        'скачать приложение', 'скачал приложение', 'скачала приложение', 'установить приложение',
        'установил приложение', 'установила приложение', 'пользоваться приложением',
        'пользуюсь приложением', 'пользуешься приложением', 'пользуется приложением',
        'зайти в приложение', 'захожу в приложение', 'заходишь в приложение', 'заходит в приложение',
        'приложение зависает', 'приложение тормозит', 'приложение не работает',
        'обновить приложение', 'обновил приложение', 'обновила приложение', 'интерфейс приложения'
    ],

    'интернет-банк': [
        'интернет-банк', 'интернет-банка', 'интернет-банку', 'интернет-банком',
        'интернет-банке', 'онлайн банк', 'онлайн банка', 'онлайн банку', 'онлайн банком',
        'личный кабинет', 'личного кабинета', 'личному кабинету', 'личным кабинетом',
        'кабинет гпб', 'кабинета гпб', 'кабинету гпб', 'кабинетом гпб',
        'войти в интернет-банк', 'вхожу в интернет-банк', 'входишь в интернет-банк',
        'входит в интернет-банк', 'зайти в личный кабинет', 'захожу в личный кабинет',
        'заходишь в личный кабинет', 'заходит в личный кабинет', 'работать через интернет-банк',
        'работаю через интернет-банк', 'работаешь через интернет-банк', 'работает через интернет-банк',
        'онлайн услуги', 'дистанционное обслуживание', 'удаленный доступ к счету'
    ],

    'инвестиции': [
        'инвестиции', 'инвестиций', 'инвестициям', 'инвестициями', 'инвестициях',
        'инвестиционный счет', 'инвестиционного счета', 'инвестиционному счету',
        'инвестиционным счетом', 'инвестиционном счете', 'иис', 'акции', 'акций',
        'акциям', 'акциями', 'акциях', 'облигации', 'облигаций', 'облигациям',
        'облигациями', 'облигациях', 'ценные бумаги', 'ценных бумаг', 'ценным бумагам',
        'инвестировать деньги', 'инвестирую деньги', 'инвестируешь деньги', 'инвестирует деньги',
        'вложить деньги', 'вложил деньги', 'вложила деньги', 'вкладывать деньги',
        'вкладываю деньги', 'вкладываешь деньги', 'вкладывает деньги', 'покупать акции',
        'покупаю акции', 'покупаешь акции', 'покупает акции', 'купить акции',
        'купил акции', 'купила акции', 'продавать акции', 'продаю акции', 'продаешь акции',
        'продает акции', 'инвестиционный портфель', 'доходность инвестиций', 'риски инвестиций'
    ],

    'ипотека': [
        'ипотека', 'ипотеки', 'ипотеке', 'ипотеку', 'ипотекой', 'ипотекою',
        'ипотечный кредит', 'ипотечного кредита', 'ипотечному кредиту',
        'ипотечным кредитом', 'кредит на жилье', 'кредита на жилье', 'кредиту на жилье',
        'кредитом на жилье', 'ипотечное кредитование', 'ипотечного кредитования',
        'взять ипотеку', 'взял ипотеку', 'взяла ипотеку', 'оформить ипотеку',
        'оформил ипотеку', 'оформила ипотеку', 'погашать ипотеку', 'погашаю ипотеку',
        'погашаешь ипотеку', 'погашает ипотеку', 'ипотечная ставка', 'ставка по ипотеке',
        'ипотечная квартира', 'ипотечное жилье', 'первоначальный взнос', 'ипотечный брокер'
    ],

    'вклады': [
        'вклад', 'вклада', 'вкладу', 'вкладом', 'вкладе', 'вклады', 'вкладов',
        'вкладам', 'вкладами', 'вкладах', 'депозит', 'депозита', 'депозиту',
        'депозитом', 'депозите', 'депозиты', 'депозитов', 'депозитам', 'депозитами',
        'срочный вклад', 'срочного вклада', 'срочному вкладу', 'срочным вкладом',
        'сберегательный вклад', 'сберегательного вклада', 'сберегательному вкладу',
        'сберегательным вкладом',
        'открыть вклад', 'открыл вклад', 'открыла вклад', 'оформить депозит',
        'оформил депозит', 'оформила депозит', 'положить деньги на вклад',
        'положил деньги на вклад', 'положила деньги на вклад', 'снять с вклада',
        'снял с вклада', 'сняла с вклада', 'проценты по вкладу', 'доходность вклада',
        'ставка по вкладу', 'срок вклада', 'пополняемый вклад', 'сберегательный счет'
    ],

    'депозитарные услуги': [
        'депозитарные услуги', 'депозитарных услуг', 'депозитарным услугам',
        'депозитарными услугами', 'депозитарных услугах', 'депозитарная услуга',
        'депозитарной услуги', 'депозитарную услугу', 'депозитарной услугой',
        'депозитарий', 'депозитария', 'депозитарию', 'депозитарием', 'депозитарии',
        'депозитарное хранение', 'депозитарного хранения', 'депозитарному хранению',
        'депозитарным хранением', 'депозитарном хранении',
        'хранить ценные бумаги', 'храню ценные бумаги', 'хранишь ценные бумаги',
        'хранит ценные бумаги', 'депозитарное обслуживание', 'учет ценных бумаг',
        'реестр владельцев', 'депозитарная расписка', 'кастодиальные услуги'
    ],

    'денежные переводы': [
        'денежные переводы', 'денежных переводов', 'денежным переводам',
        'денежными переводами', 'денежных переводах', 'денежный перевод',
        'денежного перевода', 'денежному переводу', 'денежным переводом',
        'перевод денег', 'перевода денег', 'переводу денег', 'переводом денег',
        'перевод средств', 'перевода средств', 'переводу средств', 'переводом средств',
        'отправка денег', 'отправки денег', 'отправке денег', 'отправкой денег',
        'получение денег', 'получения денег', 'получению денег', 'получением денег',
        'переводить деньги', 'перевожу деньги', 'переводишь деньги', 'переводит деньги',
        'переводим деньги', 'переводите деньги', 'переводят деньги', 'перевести деньги',
        'перевёл деньги', 'перевела деньги', 'перевели деньги', 'переводя деньги',
        'отправлять деньги', 'отправляю деньги', 'отправляешь деньги', 'отправляет деньги',
        'отправляем деньги', 'отправляете деньги', 'отправляют деньги', 'отправить деньги',
        'отправил деньги', 'отправила деньги', 'отправили деньги', 'отправляя деньги',
        'перечислять деньги', 'перечисляю деньги', 'перечисляешь деньги', 'перечисляет деньги',
        'перечисляем деньги', 'перечисляете деньги', 'перечисляют деньги', 'перечислить деньги',
        'перечислил деньги', 'перечислила деньги', 'перечислили деньги', 'перечисляя деньги',
        'перевод между картами', 'переводы между картами', 'переводу между картами',
        'межкартовый перевод', 'межкартового перевода', 'межкартовому переводу',
        'перевод с карты на карту', 'перевода с карты на карту', 'переводу с карты на карту',
        'быстрый перевод', 'быстрого перевода', 'быстрому переводу', 'быстрым переводом',
        'онлайн перевод', 'онлайн перевода', 'онлайн переводу', 'онлайн переводом',
        'система быстрых платежей', 'сбп', 'перевод по номеру телефона'
    ],

    'зарплатные карты': [
        'зарплатная карта', 'зарплатной карты', 'зарплатную карту', 'зарплатной картой',
        'зарплатные карты', 'зарплатных карт', 'зарплатным картам', 'зарплатными картами',
        'зарплатный проект', 'зарплатного проекта', 'зарплатному проекту', 'зарплатным проектом',
        'карта для зарплаты', 'карты для зарплаты', 'карту для зарплаты', 'картой для зарплаты',
        'зарплатный счет', 'зарплатного счета', 'зарплатному счету', 'зарплатным счетом',
        'получать зарплату на карту', 'получаю зарплату на карту', 'получаешь зарплату на карту',
        'получает зарплату на карту', 'зарплатный проект', 'карта для зарплаты',
        'зарплата на карту', 'начисление зарплаты', 'получение зарплаты', 'зарплатная карточка'
    ],

    'премиальные карты': [
        'премиальная карта', 'премиальной карты', 'премиальную карту', 'премиальной картой',
        'премиальные карты', 'премиальных карт', 'премиальным картам', 'премиальными картами',
        'золотая карта', 'золотой карты', 'золотую карту', 'золотой картой',
        'платиновая карта', 'платиновой карты', 'платиновую карту', 'платиновой картой',
        'премиум карта', 'премиум карты', 'премиум карту', 'премиум картой',
        'вип карта', 'вип карты', 'вип карту', 'вип картой', 'премиальный статус',
        'оформить премиальную карту', 'оформил премиальную карту', 'оформила премиальную карту',
        'пользоваться премиальной картой', 'пользуюсь премиальной картой', 'пользуешься премиальной картой',
        'премиальное обслуживание', 'вип обслуживание', 'привилегии карты', 'дополнительные услуги'
    ],

    'автокредитование': [
        'автокредит', 'автокредита', 'автокредиту', 'автокредитом', 'автокредите',
        'кредит на автомобиль', 'кредита на автомобиль', 'кредиту на автомобиль',
        'кредитом на автомобиль', 'автокредитование', 'автокредитования',
        'автокредитованию', 'автокредитованием',
        'взять автокредит', 'взял автокредит', 'взяла автокредит', 'оформить автокредит',
        'оформил автокредит', 'оформила автокредит', 'кредит на машину', 'кредит на авто',
        'покупка автомобиля в кредит', 'авто в кредит', 'автомобильное кредитование'
    ],

    'рефинансирование кредитов': [
        'рефинансирование', 'рефинансирования', 'рефинансированию', 'рефинансированием',
        'рефинансировании', 'рефинансирование кредита', 'рефинансирования кредита',
        'рефинансированию кредита', 'рефинансированием кредита', 'рефинансирование займа',
        'рефинансирования займа', 'рефинансированию займа', 'рефинансированием займа',
        'рефинансировать кредит', 'рефинансирую кредит', 'рефинансируешь кредит',
        'рефинансирует кредит', 'перекредитование', 'объединить кредиты', 'объединяю кредиты',
        'объединяешь кредиты', 'объединяет кредиты', 'снизить процентную ставку',
        'снижаю процентную ставку', 'снижаешь процентную ставку', 'снижает процентную ставку'
    ],

    'страховые и сервисные продукты': [
        'страхование', 'страхования', 'страхованию', 'страхованием', 'страховании',
        'страховка', 'страховки', 'страховку', 'страховкой', 'страховке',
        'страховой полис', 'страхового полиса', 'страховому полису', 'страховым полисом',
        'страховые продукты', 'страховых продуктов', 'страховым продуктам',
        'сервисные продукты', 'сервисных продуктов', 'сервисным продуктам',
        'дополнительные услуги', 'дополнительных услуг', 'дополнительным услугам',
        'оформить страховку', 'оформил страховку', 'оформила страховку', 'купить страховку',
        'купил страховку', 'купила страховку', 'застраховать', 'застраховал', 'застраховала',
        'страховая защита', 'страховой случай', 'страховые выплаты', 'сервисное обслуживание',
        'дополнительная услуга', 'подключить услугу', 'подключил услугу', 'подключила услугу'
    ]
}

SERVICE_CONTEXT_KEYWORDS = [
    # обслуживание - полный охват
    'обслуживание', 'обслуживания', 'обслуживанию', 'обслуживанием', 'обслуживании',
    'обслуживанье', 'обслуживанья', 'обслуживанью', 'обслуживаньем', 'обслуживаньи',
    'обслуживающий', 'обслуживающего', 'обслуживающему', 'обслуживающим', 'обслуживающем',
    'обслуживающая', 'обслуживающей', 'обслуживающую', 'обслуживающею', 'обслуживающее',
    'обслуживающие', 'обслуживающих', 'обслуживающим', 'обслуживающими',
    'обслуживать', 'обслуживаю', 'обслуживаешь', 'обслуживает', 'обслуживаем',
    'обслуживаете', 'обслуживают', 'обслуживал', 'обслуживала', 'обслуживало', 'обслуживали',
    'обслуживай', 'обслуживайте', 'обслуживая', 'обслуженный', 'обслуженного', 'обслуженному',
    'обслуженным', 'обслуженном', 'обслуженная', 'обслуженной', 'обслуженную', 'обслуженною',
    'обслуженное', 'обслуженные', 'обслуженных', 'обслуженным', 'обслуженными',
    'обслужи', 'обслужил', 'обслужила', 'обслужило', 'обслужили', 'обслужу', 'обслужишь',

    # сервис - полный охват
    'сервис', 'сервиса', 'сервису', 'сервисом', 'сервисе', 'сервисы', 'сервисов',
    'сервисам', 'сервисами', 'сервисах', 'сервисный', 'сервисного', 'сервисному',
    'сервисным', 'сервисном', 'сервисная', 'сервисной', 'сервисную', 'сервисною',
    'сервисное', 'сервисные', 'сервисных', 'сервисным', 'сервисными',

    # сотрудник - полный охват
    'сотрудник', 'сотрудника', 'сотруднику', 'сотрудником', 'сотруднике',
    'сотрудники', 'сотрудников', 'сотрудникам', 'сотрудниками', 'сотрудниках',
    'сотрудница', 'сотрудницы', 'сотруднице', 'сотрудницу', 'сотрудницей',
    'сотрудничать', 'сотрудничаю', 'сотрудничаешь', 'сотрудничает', 'сотрудничаем',
    'сотрудничаете', 'сотрудничают', 'сотрудничал', 'сотрудничала', 'сотрудничало',
    'сотрудничали', 'сотрудничая', 'сотрудничающий', 'сотрудничающего', 'сотрудничающему',

    # менеджер - полный охват
    'менеджер', 'менеджера', 'менеджеру', 'менеджером', 'менеджере',
    'менеджеры', 'менеджеров', 'менеджерам', 'менеджерами', 'менеджерах',
    'менеджерский', 'менеджерского', 'менеджерскому', 'менеджерским', 'менеджерском',
    'менеджерская', 'менеджерской', 'менеджерскую', 'менеджерскою', 'менеджерское',
    'менеджерские', 'менеджерских', 'менеджерским', 'менеджерскими',

    # консультант - полный охват
    'консультант', 'консультанта', 'консультанту', 'консультантом', 'консультанте',
    'консультанты', 'консультантов', 'консультантам', 'консультантами', 'консультантах',
    'консультация', 'консультации', 'консультацию', 'консультацией', 'консультациею',
    'консультаций', 'консультациям', 'консультациями', 'консультациях',
    'консультировать', 'консультирую', 'консультируешь', 'консультирует', 'консультируем',
    'консультируете', 'консультируют', 'консультировал', 'консультировала', 'консультировало',
    'консультировали', 'консультируй', 'консультируйте', 'консультируя',
    'проконсультировать', 'проконсультировал', 'проконсультировала', 'проконсультировало',

    # вежливый - полный охват с синонимами
    'вежливый', 'вежливого', 'вежливому', 'вежливым', 'вежливом',
    'вежливая', 'вежливой', 'вежливую', 'вежливою', 'вежливое',
    'вежливые', 'вежливых', 'вежливым', 'вежливыми', 'вежливо',
    'вежливость', 'вежливости', 'вежливостью', 'вежливостям', 'вежливостями',
    'учтивый', 'учтивого', 'учтивому', 'учтивым', 'учтивом', 'учтивая', 'учтивой',
    'учтивую', 'учтивою', 'учтивое', 'учтивые', 'учтивых', 'учтивым', 'учтивыми',
    'корректный', 'корректного', 'корректному', 'корректным', 'корректном',
    'корректная', 'корректной', 'корректную', 'корректною', 'корректное',
    'корректные', 'корректных', 'корректным', 'корректными', 'корректно',
    'обходительный', 'обходительного', 'обходительному', 'обходительным',

    # грубый - полный охват с синонимами
    'грубый', 'грубого', 'грубому', 'грубым', 'грубом',
    'грубая', 'грубой', 'грубую', 'грубою', 'грубое',
    'грубые', 'грубых', 'грубым', 'грубыми', 'грубо',
    'грубость', 'грубости', 'грубостью', 'грубостям', 'грубостями',
    'невежливый', 'невежливого', 'невежливому', 'невежливым', 'невежливом',
    'невежливая', 'невежливой', 'невежливую', 'невежливою', 'невежливое',
    'невежливые', 'невежливых', 'невежливым', 'невежливыми',
    'хамский', 'хамского', 'хамскому', 'хамским', 'хамском',
    'хамская', 'хамской', 'хамскую', 'хамскою', 'хамское',
    'хамские', 'хамских', 'хамским', 'хамскими', 'хамски',
    'хамство', 'хамства', 'хамству', 'хамством', 'хамстве',
    'некорректный', 'некорректного', 'некорректному', 'некорректным',

    # помощь - полный охват с синонимами
    'помощь', 'помощи', 'помощью', 'помощью', 'помощам', 'помощами',
    'помогать', 'помогаю', 'помогаешь', 'помогает', 'помогаем', 'помогаете', 'помогают',
    'помог', 'помогла', 'помогло', 'помогли', 'помоги', 'помогите', 'помогая',
    'помощник', 'помощника', 'помощнику', 'помощником', 'помощнике',
    'помощница', 'помощницы', 'помощнице', 'помощницу', 'помощницей',
    'подмога', 'подмоги', 'подмоге', 'подмогу', 'подмогой', 'подмогою',
    'содействие', 'содействия', 'содействию', 'содействием', 'содействии',
    'оказывать помощь', 'оказываю помощь', 'оказываешь помощь', 'оказывает помощь',
    'оказать помощь', 'оказал помощь', 'оказала помощь', 'оказало помощь',

    # поддержка - полный охват с синонимами
    'поддержка', 'поддержки', 'поддержку', 'поддержкой', 'поддержкою', 'поддержке',
    'поддерживать', 'поддерживаю', 'поддерживаешь', 'поддерживает', 'поддерживаем',
    'поддерживаете', 'поддерживают', 'поддерживал', 'поддерживала', 'поддерживало',
    'поддерживали', 'поддерживай', 'поддерживайте', 'поддерживая',
    'поддержать', 'поддержал', 'поддержала', 'поддержало', 'поддержали',
    'поддерживающий', 'поддерживающего', 'поддерживающему', 'поддерживающим',

    # внимательный - полный охват с синонимами
    'внимательный', 'внимательного', 'внимательному', 'внимательным', 'внимательном',
    'внимательная', 'внимательной', 'внимательную', 'внимательною', 'внимательное',
    'внимательные', 'внимательных', 'внимательным', 'внимательными', 'внимательно',
    'внимательность', 'внимательности', 'внимательностью', 'внимательностям',
    'заботливый', 'заботливого', 'заботливому', 'заботливым', 'заботливом',
    'заботливая', 'заботливой', 'заботливую', 'заботливою', 'заботливое',
    'чуткий', 'чуткого', 'чуткому', 'чутким', 'чутком', 'чуткая', 'чуткой',
    'чуткую', 'чуткою', 'чуткое', 'чуткие', 'чутких', 'чутким', 'чуткими',

    # профессионализм - полный охват с синонимами
    'профессионализм', 'профессионализма', 'профессионализму', 'профессионализмом',
    'профессионализме', 'профессионал', 'профессионала', 'профессионалу', 'профессионалом',
    'профессионале', 'профессионалы', 'профессионалов', 'профессионалам', 'профессионалами',
    'профессиональный', 'профессионального', 'профессиональному', 'профессиональным',
    'профессиональная', 'профессиональной', 'профессиональную', 'профессиональною',
    'профессиональное', 'профессиональные', 'профессиональных', 'профессиональным',
    'профессиональными', 'профессионально', 'квалификация', 'квалификации',
    'квалификацию', 'квалификацией', 'квалификациею', 'квалифицированный',
    'квалифицированного', 'квалифицированному', 'квалифицированным',
    'компетентный', 'компетентного', 'компетентному', 'компетентным',

    # дополнительные ключевые слова для обслуживания
    'работа', 'работы', 'работе', 'работу', 'работой', 'работою', 'работ',
    'работам', 'работами', 'работах', 'работать', 'работаю', 'работаешь',
    'работает', 'работаем', 'работаете', 'работают', 'работал', 'работала',

    'качество', 'качества', 'качеству', 'качеством', 'качестве', 'качеств',
    'качествам', 'качествами', 'качествах', 'качественный', 'качественного',

    'отношение', 'отношения', 'отношению', 'отношением', 'отношении', 'отношений',
    'отношениям', 'отношениями', 'отношениях', 'относиться', 'отношусь',
    'относишься', 'относится', 'относимся', 'относитесь', 'относятся',

    'общение', 'общения', 'общению', 'общением', 'общении', 'общений',
    'общениям', 'общениями', 'общениях', 'общаться', 'общаюсь', 'общаешься',

    'решение', 'решения', 'решению', 'решением', 'решении', 'решений',
    'решениям', 'решениями', 'решениях', 'решать', 'решаю', 'решаешь',

    'обращение', 'обращения', 'обращению', 'обращением', 'обращении', 'обращений',
    'обращениям', 'обращениями', 'обращениях', 'обращаться', 'обращаюсь',

    'прием', 'приема', 'приему', 'приемом', 'приеме', 'приемы', 'приемов',
    'приемам', 'приемами', 'приемах', 'принимать', 'принимаю', 'принимаешь',

    'отделение', 'отделения', 'отделению', 'отделением', 'отделении', 'отделений',
    'отделениям', 'отделениями', 'отделениях', 'офис', 'офиса', 'офису',

    'филиал', 'филиала', 'филиалу', 'филиалом', 'филиале', 'филиалы', 'филиалов',
    'филиалам', 'филиалами', 'филиалах', 'банк', 'банка', 'банку', 'банком',

    'клиент', 'клиента', 'клиенту', 'клиентом', 'клиенте', 'клиенты', 'клиентов',
    'клиентам', 'клиентами', 'клиентах', 'клиентский', 'клиентского',

    'проблема', 'проблемы', 'проблеме', 'проблему', 'проблемой', 'проблемою',
    'проблем', 'проблемам', 'проблемами', 'проблемах', 'решать проблему',

    'вопрос', 'вопроса', 'вопросу', 'вопросом', 'вопросе', 'вопросы', 'вопросов',
    'вопросам', 'вопросами', 'вопросах', 'задать вопрос', 'задаю вопрос',

    'жалоба', 'жалобы', 'жалобе', 'жалобу', 'жалобой', 'жалобою', 'жалоб',
    'жалобам', 'жалобами', 'жалобах', 'жаловаться', 'жалуюсь', 'жалуешься',

    'претензия', 'претензии', 'претензию', 'претензией', 'претензиею', 'претензий',
    'претензиям', 'претензиями', 'претензиях', 'предъявить претензию',

    'рекламация', 'рекламации', 'рекламацию', 'рекламацией', 'рекламациею',
    'рекламаций', 'рекламациям', 'рекламациями', 'рекламациях',

    'отзыв', 'отзыва', 'отзыву', 'отзывом', 'отзыве', 'отзывы', 'отзывов',
    'отзывам', 'отзывами', 'отзывах', 'оставить отзыв', 'оставляю отзыв',

    # глаголы обслуживания
    'обслужил', 'обслужила', 'обслужили', 'обслужу', 'обслужишь', 'обслужит',
    'обслужим', 'обслужите', 'обслужат', 'обслуживал', 'обслуживала', 'обслуживало',
    'обслуживали', 'обслужено', 'обслужен', 'обслужена', 'обслужены',

    'помог', 'помогла', 'помогли', 'помогло', 'помогу', 'поможешь', 'поможет',
    'поможем', 'поможете', 'помогут', 'помогал', 'помогала', 'помогало', 'помогали',

    'поддержал', 'поддержала', 'поддержали', 'поддержало', 'поддержу', 'поддержишь',
    'поддержит', 'поддержим', 'поддержите', 'поддержат', 'поддерживал', 'поддерживала',

    'проконсультировал', 'проконсультировала', 'проконсультировали', 'проконсультирую',
    'проконсультируешь', 'проконсультирует', 'проконсультируем', 'проконсультируете',

    'решил', 'решила', 'решили', 'решило', 'решу', 'решишь', 'решит', 'решим',
    'решите', 'решат', 'решал', 'решала', 'решало', 'решали', 'решая',

    'ответил', 'ответила', 'ответили', 'ответило', 'отвечу', 'ответишь', 'ответит',
    'ответим', 'ответите', 'ответят', 'отвечал', 'отвечала', 'отвечало', 'отвечали'
]



def detect_gender_from_text(text):
    text_lower = text.lower()
    female_patterns = [r'\b(?:пришла|получила|оформила|позвонила|написала|сказала|сделала|увидела|узнала|решила)\b']
    male_patterns = [r'\b(?:пришел|получил|оформил|позвонил|написал|сказал|сделал|увидел|узнал|решил)\b']
    female_matches = sum(len(re.findall(pattern, text_lower)) for pattern in female_patterns)
    male_matches = sum(len(re.findall(pattern, text_lower)) for pattern in male_patterns)
    if female_matches > male_matches:
        return 'женщина'
    elif male_matches > female_matches:
        return 'мужчина'
    else:
        return 'не определен'


def load_optimized_bert_model():
    """🔥 ОПТИМИЗИРОВАННАЯ загрузка BERT модели"""
    try:
        # 🔥 ПРИОРИТЕТ: самая быстрая модель
        try:
            #model = SentenceTransformer('sentence-transformers/paraphrase-albert-small-v2',
            model = SentenceTransformer('sentence-transformers/all-MiniLM-L6-v2',
                                        device='cuda' if torch.cuda.is_available() else 'cpu')
            print_success("Загружена ОПТИМИЗИРОВАННАЯ BERT модель (Albert Small)")
            return model
        except:
            # 🔥 РЕЗЕРВ: вторая быстрая модель
            try:
                model = SentenceTransformer('sentence-transformers/all-MiniLM-L6-v2',
                                            device='cuda' if torch.cuda.is_available() else 'cpu')
                print_success("Загружена резервная легкая модель (MiniLM-L6)")
                return model
            except:
                # 🔥 ФОЛБЭК: оригинальная модель
                return load_bert_model()

    except Exception as e:
        print_error(f"Ошибка загрузки оптимизированной BERT модели: {e}")
        return load_bert_model()


def ultra_fast_bert_segmentation(text, bert_model, verbose=False):
    """🔥 УЛЬТРА-БЫСТРАЯ BERT сегментация"""
    # Быстрая проверка для коротких текстов
    if len(text) < 150:
        entities = detect_entities_with_positions(text)
        primary_entity = entities[0]['entity'] if entities else 'общее_обслуживание'
        return [{
            'text': text,
            'entity_type': primary_entity,
            'service_channel': None,
            'combined_entity_name': primary_entity,
            'entity_category': 'fast_track',
            'is_entity_specific': bool(entities),
            'has_service_context': contains_service_context(text),
            'confidence': 'high',
            'segmentation_method': 'ultra_fast',
            'semantic_coherence': 1.0,
            'detection_method': 'keyword_only'
        }]

    # Ограничение длины для скорости
    if len(text) > 4000:
        text = text[:4000] + "..."

    return advanced_bert_segmentation(text, bert_model, verbose)

#def load_bert_model():
#    try:
#        model = SentenceTransformer('sentence-transformers/paraphrase-multilingual-MiniLM-L12-v2',
#                                    device='cuda' if torch.cuda.is_available() else 'cpu')
#        print_success("Загружена BERT модель для семантического анализа")
#        return model
#    except Exception as e:
#        print_error(f"Ошибка загрузки BERT модели: {e}")
#        return None

def load_bert_model():
    try:
        # 🔥 ЗАМЕНА на ультра-легкую модель (в 4 раза быстрее!)
        #model = SentenceTransformer('sentence-transformers/paraphrase-albert-small-v2',
        model=SentenceTransformer('sentence-transformers/all-MiniLM-L6-v2',
                                    device='cuda' if torch.cuda.is_available() else 'cpu')
        print_success("Загружена ОПТИМИЗИРОВАННАЯ BERT модель (Albert Small)")
        return model
    except Exception as e:
        # Fallback к еще более легкой модели
        try:
            model = SentenceTransformer('sentence-transformers/all-MiniLM-L6-v2',
                                        device='cuda' if torch.cuda.is_available() else 'cpu')
            print_success("Загружена резервная легкая модель (MiniLM-L6)")
            return model
        except Exception as e2:
            print_error(f"Ошибка загрузки BERT модели: {e2}")
            return None

def enhanced_sentence_splitting(text):
    try:
        sentences = nltk.tokenize.sent_tokenize(text, language='russian')
    except:
        sentences = re.split(r'(?<=[.!?])\s+', text)
    return [s.strip() for s in sentences if len(s.strip()) > 0]


def detect_entities_with_positions(text):
    """Улучшенное обнаружение ВСЕХ сущностей"""
    entities_found = []
    text_lower = text.lower()

    # Основные продукты
    for product, keywords in PRODUCT_ENTITIES.items():
        for keyword in keywords:
            pattern = r'\b' + re.escape(keyword) + r'\b'
            matches = list(re.finditer(pattern, text_lower))
            for match in matches:
                entities_found.append({
                    'entity': product, 'keyword': keyword,
                    'position': match.start(), 'length': len(keyword)
                })

    # Важно: добавляем обнаружение обслуживания
    service_indicators = ['обслуживание', 'сервис', 'сотрудник', 'менеджер']
    for indicator in service_indicators:
        matches = list(re.finditer(r'\b' + indicator + r'\w*\b', text_lower))
        for match in matches:
            entities_found.append({
                'entity': 'общее_обслуживание',
                'keyword': match.group(),
                'position': match.start(),
                'length': len(match.group())
            })

    # Убираем дубликаты
    unique_entities = []
    seen = set()
    for entity in sorted(entities_found, key=lambda x: x['position']):
        key = (entity['position'], entity['entity'])
        if key not in seen:
            unique_entities.append(entity)
            seen.add(key)

    return unique_entities


def find_natural_boundaries(text, entities):
    boundaries = []
    if len(entities) < 2: return boundaries
    for i in range(len(entities) - 1):
        current_entity = entities[i]
        next_entity = entities[i + 1]
        gap_start = current_entity['position'] + current_entity['length']
        gap_end = next_entity['position']
        gap_text = text[gap_start:gap_end]
        best_boundary = find_best_split_point(gap_text, gap_start)
        if best_boundary: boundaries.append(best_boundary)
    return sorted(boundaries)


def find_best_split_point(gap_text, gap_start):
    gap_lower = gap_text.lower()
    split_points = []
    semicolon_pos = gap_text.find(';')
    if semicolon_pos != -1: split_points.append((gap_start + semicolon_pos + 1, 10))
    comma_positions = [m.start() for m in re.finditer(',', gap_text)]
    for pos in comma_positions:
        if pos + 1 < len(gap_text) and gap_text[pos + 1] == ' ':
            split_points.append((gap_start + pos + 1, 8))
    conjunctions = [' а ', ' но ', ' да и ', ' и ']
    for conj in conjunctions:
        conj_pos = gap_lower.find(conj)
        if conj_pos != -1: split_points.append((gap_start + conj_pos, 5))
    if split_points:
        split_points.sort(key=lambda x: (-x[1], x[0]))
        return split_points[0][0]
    if len(gap_text) > 5: return gap_start + len(gap_text) // 2
    return None


def contains_service_context(text):
    if not text: return False
    text_lower = text.lower()
    service_indicators = 0
    for keyword in SERVICE_CONTEXT_KEYWORDS:
        pattern = r'\b' + re.escape(keyword) + r'\w*\b'
        matches = re.findall(pattern, text_lower)
        service_indicators += len(matches)
    word_count = len(text.split())
    threshold = max(1, word_count // 10)
    return service_indicators >= threshold


def bert_semantic_analysis(text, bert_model):
    """BERT АНАЛИЗ с кэшированием эмбеддингов"""
    if bert_model is None:
        return {'segments': [{'text': text, 'semantic_label': 'unknown'}]}

    sentences = enhanced_sentence_splitting(text)
    if len(sentences) <= 1:
        return {'segments': [{'text': text, 'semantic_label': 'single_sentence'}]}

    try:
        # Проверяем кэш для каждого предложения
        sentence_embeddings = []
        sentences_to_process = []
        sentence_indices = []

        for i, sentence in enumerate(sentences):
            sentence_hash = get_text_hash(sentence)
            if sentence_hash in bert_embedding_cache:
                cached_embedding = bert_embedding_cache[sentence_hash]
                if cached_embedding.device != bert_model.device:
                    cached_embedding = cached_embedding.to(bert_model.device)
                sentence_embeddings.append(cached_embedding)
            else:
                sentences_to_process.append(sentence)
                sentence_indices.append(i)
                sentence_embeddings.append(None)

        # Обрабатываем только новые предложения
        if sentences_to_process:
            new_embeddings = bert_model.encode(
                sentences_to_process,
                convert_to_tensor=True,
                show_progress_bar=False,
                batch_size=min(32, len(sentences_to_process)),
                normalize_embeddings=True
            )

            for idx, (sentence, embedding) in enumerate(zip(sentences_to_process, new_embeddings)):
                sentence_hash = get_text_hash(sentence)
                bert_embedding_cache[sentence_hash] = embedding.cpu()
                sentence_embeddings[sentence_indices[idx]] = embedding

        # Собираем все эмбеддинги
        final_embeddings = []
        for embedding in sentence_embeddings:
            if embedding is not None:
                final_embeddings.append(embedding)

        if not final_embeddings:
            return {'segments': [{'text': text, 'semantic_label': 'error'}]}

        sentence_embeddings_tensor = torch.stack(final_embeddings)

        # Вычисляем сходство
        similarity_matrix = util.pytorch_cos_sim(sentence_embeddings_tensor, sentence_embeddings_tensor)

        # Динамический порог
        all_similarities = []
        for i in range(len(sentences)):
            for j in range(i + 1, len(sentences)):
                all_similarities.append(similarity_matrix[i][j].item())

        if all_similarities:
            avg_similarity = np.mean(all_similarities)
            std_similarity = np.std(all_similarities)
            dynamic_threshold = max(0.2, avg_similarity - std_similarity)
        else:
            dynamic_threshold = 0.3

        # Сегментация
        segments = []
        current_segment = [sentences[0]]
        current_embedding = sentence_embeddings_tensor[0].unsqueeze(0)

        for i in range(1, len(sentences)):
            similarity = util.pytorch_cos_sim(sentence_embeddings_tensor[i].unsqueeze(0),
                                              current_embedding.mean(dim=0))
            similarity_score = similarity.item()

            if similarity_score > dynamic_threshold:
                current_segment.append(sentences[i])
                current_embedding = torch.cat([current_embedding, sentence_embeddings_tensor[i].unsqueeze(0)])
            else:
                segment_text = ' '.join(current_segment)
                segments.append({
                    'text': segment_text,
                    'sentences': current_segment.copy(),
                    'semantic_coherence': similarity_score,
                    'segment_id': len(segments)
                })
                current_segment = [sentences[i]]
                current_embedding = sentence_embeddings_tensor[i].unsqueeze(0)

        if current_segment:
            segment_text = ' '.join(current_segment)
            segments.append({
                'text': segment_text,
                'sentences': current_segment,
                'semantic_coherence': 1.0,
                'segment_id': len(segments)
            })

        return {'segments': segments, 'similarity_matrix': similarity_matrix, 'threshold': dynamic_threshold}

    except Exception as e:
        print_warning(f"Ошибка BERT анализа: {e}")
        return {'segments': [{'text': text, 'semantic_label': 'error'}]}


def improved_bert_segmentation(text, bert_model, verbose=False):
    """🔥 УЛУЧШЕННАЯ сегментация с безопасным BERT анализом"""

    # 1. УЛУЧШЕННОЕ обнаружение сущностей с позициями
    entities = detect_entities_with_positions(text)

    if verbose:
        print(f"   🔍 Обнаружено сущностей: {len(entities)}")
        for entity in entities:
            print(f"   ├── '{entity['keyword']}' ({entity['entity']}) на позиции {entity['position']}")

    # 2. ЕСЛИ нашли РАЗНЫЕ сущности - используем ПРАВИЛА вместо BERT (БЫСТРЕЕ)
    if len(entities) >= 2:
        entities_sorted = sorted(entities, key=lambda x: x['position'])

        # БЫСТРЫЙ поиск границ на основе правил
        boundaries = []
        for i in range(len(entities_sorted) - 1):
            current = entities_sorted[i]
            next_ent = entities_sorted[i + 1]

            gap_start = current['position'] + current['length']
            gap_end = next_ent['position']
            gap_text = text[gap_start:gap_end]

            # БЫСТРЫЙ алгоритм поиска точки разделения
            boundary = find_best_split_point_fast(gap_text, gap_start)
            if boundary:
                boundaries.append(boundary)

        if boundaries:
            segments = []
            start_pos = 0
            boundaries = sorted(boundaries)

            for boundary in boundaries:
                segment_text = text[start_pos:boundary].strip()
                if len(segment_text) > 3:
                    # БЫСТРОЕ определение сущностей в сегменте
                    seg_entities = set()
                    for entity in entities_sorted:
                        if start_pos <= entity['position'] < boundary:
                            seg_entities.add(entity['entity'])

                    segments.append({
                        'text': segment_text,
                        'sentences': [segment_text],
                        'semantic_coherence': 1.0,
                        'segment_id': len(segments),
                        'entities': list(seg_entities)
                    })
                start_pos = boundary

            # Последний сегмент
            if start_pos < len(text):
                segment_text = text[start_pos:].strip()
                if len(segment_text) > 3:
                    seg_entities = set()
                    for entity in entities_sorted:
                        if entity['position'] >= start_pos:
                            seg_entities.add(entity['entity'])

                    segments.append({
                        'text': segment_text,
                        'sentences': [segment_text],
                        'semantic_coherence': 1.0,
                        'segment_id': len(segments),
                        'entities': list(seg_entities)
                    })

            if verbose:
                print(f"   📊 Быстрое разделение на {len(segments)} сегментов")

            return {'segments': segments, 'threshold': 0.5}

    # 3. Если не сработали правила - используем БЕЗОПАСНЫЙ BERT
    return bert_semantic_analysis(text, bert_model)


def find_best_split_point_fast(gap_text, gap_start):
    """БЫСТРАЯ версия поиска точки разделения"""
    if len(gap_text) < 5:
        return None

    # Приоритеты: ; , союзы
    if ';' in gap_text:
        pos = gap_text.find(';')
        return gap_start + pos + 1  # После точки с запятой

    # Запятые с пробелом после
    comma_match = re.search(r',\s+', gap_text)
    if comma_match:
        return gap_start + comma_match.end()

    # Союзы
    for conj in [' а ', ' но ', ' да и ']:
        if conj in gap_text.lower():
            pos = gap_text.lower().find(conj)
            return gap_start + pos

    # Разделение посередине для длинных промежутков
    if len(gap_text) > 15:
        return gap_start + len(gap_text) // 2

    return None

@lru_cache(maxsize=1000)
def cached_detect_entities(segment_text):
    """Кэшированная версия обнаружения сущностей"""
    if not segment_text: return []
    segment_lower = segment_text.lower()
    entities_found = []
    for product, keywords in PRODUCT_ENTITIES.items():
        for keyword in keywords:
            pattern = r'\b' + re.escape(keyword) + r'\b'
            if re.search(pattern, segment_lower):
                entities_found.append(product)
                break
    return entities_found


def detect_entities_with_bert(segment_text, bert_model):
    if not segment_text or len(segment_text.strip()) < 3:
        return [{'entity_type': 'общее', 'combined_name': 'общее', 'entity_category': 'general',
                 'has_service_context': False, 'confidence': 'low', 'detection_method': 'fallback'}]

    # Быстрая проверка через кэш
    cached_entities = cached_detect_entities(segment_text)
    if cached_entities:
        return [{
            'entity_type': cached_entities[0],
            'combined_name': cached_entities[0],
            'entity_category': 'product_only',
            'has_service_context': contains_service_context(segment_text),
            'confidence': 'high',
            'detection_method': 'cached_keyword'
        }]

    # Полная обработка для сложных случаев
    segment_lower = segment_text.lower()
    entities_found = []
    for product, keywords in PRODUCT_ENTITIES.items():
        for keyword in keywords:
            pattern = r'\b' + re.escape(keyword) + r'\b'
            if re.search(pattern, segment_lower):
                entities_found.append(product)
                break

    if not entities_found:
        entities_found.append('общее')

    result_entities = []
    for entity in set(entities_found):
        has_service_context = contains_service_context(segment_text)
        if entity == "общее_обслуживание":
            entity_category = 'general_service'
            confidence = 'high' if has_service_context else 'medium'
        elif entity in PRODUCT_ENTITIES:
            entity_category = 'product_with_service' if has_service_context else 'product_only'
            confidence = 'high'
        else:
            entity_category = 'general'
            confidence = 'low'

        result_entities.append({
            'entity_type': entity,
            'combined_name': entity,
            'entity_category': entity_category,
            'has_service_context': has_service_context,
            'confidence': confidence,
            'detection_method': 'keyword_based'
        })

    return result_entities


def advanced_bert_segmentation(review_text, bert_model, segmenter=None, verbose=False):
    """
    УЛУЧШЕННАЯ сегментация с поддержкой профессионального сегментатора
    """
    # Если передан профессиональный сегментатор - используем его
    if segmenter:
        return professional_bert_segmentation(review_text, segmenter, verbose)

    # 🔥 ИСПРАВЛЕНИЕ: Добавляем проверку для коротких текстов
    if len(review_text) < 100:
        entities = detect_entities_with_positions(review_text)
        if len(entities) == 1:
            return [{
                'text': review_text,
                'entity_type': entities[0]['entity'],
                'service_channel': None,
                'combined_entity_name': entities[0]['entity'],
                'entity_category': 'product_only',
                'is_entity_specific': True,
                'has_service_context': contains_service_context(review_text),
                'confidence': 'high',
                'segmentation_method': 'fast_track',
                'semantic_coherence': 1.0,
                'detection_method': 'fast_keyword'
            }]

    # УЛУЧШЕННАЯ сегментация (ваш существующий код)
    analysis_result = improved_bert_segmentation(review_text, bert_model, verbose)
    segments = analysis_result['segments']

    # Пост-обработка для сохранения всех сущностей
    segments = post_process_segmentation(segments, review_text)

    split_reviews = []

    for segment in segments:
        segment_text = segment['text']
        entities = detect_entities_with_bert(segment_text, bert_model)

        # УЛУЧШЕННАЯ логика выбора сущности
        if len(entities) > 1:
            # Приоритет продуктам над обслуживанием
            product_entities = [e for e in entities
                                if e['entity_type'] in PRODUCT_ENTITIES]
            service_entities = [e for e in entities
                                if e['entity_type'] == 'общее_обслуживание']

            if product_entities:
                entities = [product_entities[0]]  # Берем первый продукт
            elif service_entities:
                entities = [service_entities[0]]
            else:
                entities = [entities[0]]  # Берем первую сущность

        for entity_info in entities:
            split_review = {
                'text': segment_text,
                'entity_type': entity_info['entity_type'],
                'service_channel': None,
                'combined_entity_name': entity_info['combined_name'],
                'entity_category': entity_info['entity_category'],
                'is_entity_specific': entity_info['entity_type'] in PRODUCT_ENTITIES,
                'has_service_context': entity_info['has_service_context'],
                'confidence': entity_info['confidence'],
                'segmentation_method': 'enhanced_bert_semantic',
                'semantic_coherence': segment.get('semantic_coherence', 1.0),
                'detection_method': entity_info.get('detection_method', 'unknown')
            }
            split_reviews.append(split_review)

    return split_reviews


def post_process_segmentation(segments, original_text):
    """Пост-обработка результатов сегментации"""
    if not segments:
        return segments

    # 1. Находим все сущности в исходном тексте
    all_entities = set([e['entity'] for e in detect_entities_with_positions(original_text)])

    # 2. Находим сущности в сегментах
    segments_entities = set()
    for segment in segments:
        segment_entities = detect_entities_with_bert(segment['text'], None)
        segment_entity_types = [e['entity_type'] for e in segment_entities]
        segments_entities.update(segment_entity_types)

    # 3. Если какие-то сущности потерялись
    missing_entities = all_entities - segments_entities
    if missing_entities:
        segments = redistribute_missing_entities(segments, missing_entities, original_text)

    return segments


def redistribute_missing_entities(segments, missing_entities, original_text):
    """Перераспределение сегментов для включения потерянных сущностей"""
    if not missing_entities:
        return segments

    new_segments = []
    i = 0
    while i < len(segments):
        current_segment = segments[i]
        current_text = current_segment['text']
        current_entities = set([e['entity_type'] for e in detect_entities_with_bert(current_text, None)])

        should_merge = False
        if i < len(segments) - 1:
            next_segment = segments[i + 1]
            next_entities = set([e['entity_type'] for e in detect_entities_with_bert(next_segment['text'], None)])

            if (current_entities.intersection(missing_entities) and
                    next_entities.intersection(missing_entities)):
                should_merge = True

        if should_merge:
            merged_text = current_text + ' ' + next_segment['text']
            new_segment = {
                'text': merged_text,
                'sentences': current_segment.get('sentences', [current_text]) +
                             next_segment.get('sentences', [next_segment['text']]),
                'semantic_coherence': min(current_segment.get('semantic_coherence', 0.5),
                                          next_segment.get('semantic_coherence', 0.5)),
                'segment_id': len(new_segments)
            }
            new_segments.append(new_segment)
            i += 2
        else:
            new_segments.append(current_segment)
            i += 1

    return new_segments


def validate_split_results(split_reviews, original_text):
    """Проверяет качество разделения"""
    if not split_reviews:
        return False

    reconstructed = ' '.join([r['text'] for r in split_reviews])
    similarity = fuzz.ratio(original_text.lower(), reconstructed.lower())

    return similarity >= 80


def find_text_column(df):
    """Находит столбец с текстом отзывов"""
    text_keywords = ['текст', 'text', 'отзыв', 'review', 'comment', 'комментарий']
    for col in df.columns:
        col_lower = col.lower()
        if any(keyword in col_lower for keyword in text_keywords):
            return col

    for col in df.columns:
        if df[col].dtype == 'object' and df[col].str.len().mean() > 20:
            return col

    return df.columns[0] if len(df.columns) > 0 else None


def process_batch_parallel(batch_data, bert_model, segmenter=None, max_workers=None):
    """
    Обрабатывает батч отзывов параллельно с поддержкой сегментатора
    """
    if max_workers is None:
        max_workers = min(multiprocessing.cpu_count(), 8)

    results = []

    print_info(f"🔄 Запуск многопоточной обработки с {max_workers} потоками")

    # Подготавливаем аргументы для каждого отзыва
    tasks = [(idx, row, text_column, bert_model, segmenter) for idx, row, text_column in batch_data]

    # Используем ThreadPoolExecutor для I/O bound операций
    with ThreadPoolExecutor(max_workers=max_workers) as executor:
        # Отправляем все задачи на выполнение
        future_to_task = {executor.submit(process_single_review, task): task for task in tasks}

        # Собираем результаты с прогресс-баром
        for future in tqdm(as_completed(future_to_task), total=len(tasks), desc="Обработка батча"):
            try:
                batch_results = future.result()
                results.extend(batch_results)
            except Exception as e:
                print_error(f"Ошибка в потоке: {e}")
                continue

    return results


def process_dataset_parallel(input_file, output_file, bert_model, segmenter=None, batch_size=50, max_workers=None,
                             show_examples=False):
    """Параллельная обработка датасета с поддержкой профессионального сегментатора"""
    print_step("ПАРАЛЛЕЛЬНАЯ ОБРАБОТКА ДАТАСЕТА")

    if segmenter:
        print("🎯 Используется профессиональный сегментатор")
    else:
        print("🤖 Используется стандартная BERT сегментация")

    try:
        df = pd.read_csv(input_file, sep=';', encoding='windows-1251', on_bad_lines='warn')
        text_column = find_text_column(df)
        print_success(f"📊 Загружено {len(df)} отзывов")
    except Exception as e:
        print_error(f"❌ Ошибка загрузки: {e}")
        return False

    if show_examples:
        if segmenter:
            demonstrate_professional_analysis(segmenter, bert_model)
        else:
            demonstrate_bert_analysis(bert_model)

    all_results = []
    original_columns = [col for col in df.columns if col != text_column]

    # Разбиваем на батчи для эффективной обработки
    batches = []
    current_batch = []

    for idx, row in df.iterrows():
        current_batch.append((idx, row, text_column))
        if len(current_batch) >= batch_size:
            batches.append(current_batch)
            current_batch = []

    if current_batch:
        batches.append(current_batch)

    print_info(f"📦 Разделено на {len(batches)} батчей по {batch_size} элементов")

    # Обрабатываем батчи последовательно (чтобы не перегружать память)
    for i, batch_data in enumerate(batches):
        print_info(f"🔧 Обработка батча {i + 1}/{len(batches)}")

        start_time = time.time()
        batch_results = process_batch_parallel(batch_data, bert_model, segmenter, max_workers)
        batch_time = time.time() - start_time

        all_results.extend(batch_results)

        print_success(f"✅ Батч {i + 1} обработан за {batch_time:.2f} сек. ({len(batch_results)} записей)")

    # Создаем итоговый DataFrame
    result_df = pd.DataFrame(all_results)

    try:
        result_df.to_csv(output_file, sep=';', index=False, encoding='windows-1251')
        print_success(f"💾 Результаты сохранены в {output_file}")

        print("\n📈 СТАТИСТИКА ОБРАБОТКИ:")
        print(f"   • Исходные отзывы: {len(df)}")
        print(f"   • Результирующие записи: {len(result_df)}")
        print(f"   • Коэффициент расширения: {len(result_df) / len(df):.2f}x")
        print(f"   • Режим: {'Профессиональная сегментация' if segmenter else 'Стандартная BERT сегментация'}")

        print(f"\n👥 РАСПРЕДЕЛЕНИЕ ОПРЕДЕЛЕННОГО ПОЛА:")
        gender_stats = result_df['detected_gender'].value_counts()
        for gender, count in gender_stats.items():
            pct = count / len(result_df) * 100
            print(f"   • {gender}: {count} ({pct:.1f}%)")

        print(f"\n🏦 РАСПРЕДЕЛЕНИЕ СУЩНОСТЕЙ:")
        entity_stats = result_df['entity_type'].value_counts().head(10)
        for entity, count in entity_stats.items():
            pct = count / len(result_df) * 100
            print(f"   • {entity}: {count} ({pct:.1f}%)")

        return True

    except Exception as e:
        print_error(f"❌ Ошибка сохранения: {e}")
        return False


def demonstrate_bert_analysis(bert_model):
    """Демонстрация работы УЛУЧШЕННОГО BERT анализа"""
    print_step("ДЕМОНСТРАЦИЯ УЛУЧШЕННОГО BERT СЕМАНТИЧЕСКОГО АНАЛИЗА")

    test_cases = [
        "обслуживание в офисе ничего особенного, а вот вклад шикарен да и страхование тоже хорошее",
        "обслуживание в офисе ничего особенного, вклад шикарен да и страхование тоже хорошее",
        "кредитная карта отличная, ставка маленькая, а мобильное приложение отвратительное",
        "Менеджер помог оформить дебетовую карту, все объяснил, но потом комиссия оказалась высокой"
    ]

    for i, text in enumerate(test_cases, 1):
        print(f"\n🧪 ТЕСТ {i}:")
        print(f"   📝 Текст: {text}")

        entities = detect_entities_with_positions(text)
        print(f"   🔍 Обнаруженные сущности с позициями:")
        for entity in entities:
            print(f"   ├── {entity['entity']} (позиция: {entity['position']})")

        points = get_optimal_segmentation_points(text, entities)
        print(f"   📍 Оптимальные точки сегментации: {points}")

        analysis = improved_bert_segmentation(text, bert_model)
        print(f"   🔍 Улучшенный BERT нашел {len(analysis['segments'])} семантических сегментов:")

        for j, segment in enumerate(analysis['segments'], 1):
            print(f"   ├── Сегмент {j}: {segment['text'][:60]}...")
            print(f"   │   Семантическая согласованность: {segment.get('semantic_coherence', 0):.2f}")
            if 'entities' in segment:
                print(f"   │   Сущности в сегменте: {segment['entities']}")

        result = advanced_bert_segmentation(text, bert_model)
        print(f"   🎯 Итоговое разделение: {len(result)} частей")
        for r in result:
            print(f"   ├── [{r['entity_type']}] {r['text'][:50]}...")


def interactive_bert_analysis(bert_model, segmenter=None):
    """Интерактивный анализ с выбором сегментатора"""
    print_step("ИНТЕРАКТИВНЫЙ АНАЛИЗ ОТЗЫВОВ")

    if segmenter:
        print("✅ Доступен профессиональный сегментатор")
        use_professional = input("Использовать профессиональный сегментатор? (y/n): ").strip().lower()
        use_professional = use_professional in ['y', 'yes', 'да', 'д']
    else:
        use_professional = False
        print("⚠️ Профессиональный сегментатор недоступен, используется стандартный")

    while True:
        print("\n" + "=" * 70)
        text = input("📝 Введите отзыв для анализа (или 'выход'): ").strip()

        if text.lower() in ['выход', 'exit', 'quit']:
            break

        if not text:
            continue

        try:
            print("\n🔬 Анализируем текст...")

            if use_professional and segmenter:
                result = professional_bert_segmentation(text, segmenter, verbose=True)
                method = "Профессиональная сегментация"
            else:
                # 🔥 ИСПРАВЛЕНИЕ: передаем segmenter как None в advanced_bert_segmentation
                result = advanced_bert_segmentation(text, bert_model, segmenter=None)
                method = "Стандартная BERT сегментация"

            print(f"✅ {method}: {len(result)} сегментов")

            print_entity_result(text, result)

            print(f"\n🔍 Детали анализа ({method}):")
            for i, item in enumerate(result, 1):
                method_icon = "🎯" if use_professional else "🤖"
                coherence = item.get('semantic_coherence', 0)
                confidence_icon = "🔴" if item['confidence'] == 'low' else "🟡" if item[
                                                                                     'confidence'] == 'medium' else "🟢"
                print(
                    f"   {i}. {method_icon} {confidence_icon} [{item['entity_type']}] (согласованность: {coherence:.2f})")
                print(f"      {item['text'][:80]}...")

        except Exception as e:
            print_error(f"Ошибка анализа: {e}")

        cont = input("\n▶️  Продолжить анализ? (y/n): ").strip().lower()
        if cont not in ['y', 'yes', 'да', 'д']:
            break


def print_entity_result(text, entities):
    print(f"\n📋 Результат анализа отзыва:")
    print(f"   📝 Текст: \"{text[:200]}{'...' if len(text) > 200 else ''}\"")
    if entities:
        print("   🏷️  Обнаруженные сущности:")
        for entity in entities:
            confidence_icon = "🔴" if entity['confidence'] == 'low' else "🟡" if entity['confidence'] == 'medium' else "🟢"
            service_icon = "👥" if entity['has_service_context'] else "📦"
            print(f"      {confidence_icon} {service_icon} {entity['entity_type']} ({entity['entity_category']})")
    else:
        print("   ❌ Сущности не обнаружены")
    print("-" * 80)


def get_optimal_segmentation_points(text, entities):
    """Определение оптимальных точек сегментации на основе сущностей"""
    segmentation_points = []

    if not entities:
        return segmentation_points

    # Добавляем точки между разными сущностями
    for i in range(1, len(entities)):
        current_entity = entities[i]
        prev_entity = entities[i - 1]

        # Если сущности разные и между ними достаточное расстояние
        if (current_entity['entity'] != prev_entity['entity'] and
                current_entity['position'] - (prev_entity['position'] + prev_entity['length']) > 10):

            # Ищем естественную границу между сущности
            gap_start = prev_entity['position'] + prev_entity['length']
            gap_end = current_entity['position']
            gap_text = text[gap_start:gap_end].lower()

            # Ищем знаки препинания или союзы как точки разделения
            boundary_chars = [',', ';', 'а', 'но', 'да и', 'и']
            boundary_pos = -1

            for boundary in boundary_chars:
                pos = gap_text.find(boundary)
                if pos != -1 and (boundary_pos == -1 or pos < boundary_pos):
                    boundary_pos = pos

            if boundary_pos != -1:
                segmentation_points.append(gap_start + boundary_pos)

    return segmentation_points


def process_single_review(args):
    idx, row, text_column, bert_model, segmenter = args  # Добавляем segmenter
    try:
        text = str(row[text_column])
        if not text or text.lower() in ['nan', 'none', '']:
            return []
        detected_gender = detect_gender_from_text(text)

        # Используем профессиональный сегментатор если доступен
        if segmenter:
            split_reviews = professional_bert_segmentation(text, segmenter)
        else:
            with bert_lock:
                split_reviews = advanced_bert_segmentation(text, bert_model, segmenter=None)

        results = []
        for review in split_reviews:
            new_row = {
                'original_review_id': idx,
                'text': review['text'],
                'entity_type': review['entity_type'],
                'service_channel': review['service_channel'],
                'combined_entity_name': review['combined_entity_name'],
                'entity_category': review['entity_category'],
                'is_entity_specific': review['is_entity_specific'],
                'has_service_context': review['has_service_context'],
                'confidence': review['confidence'],
                'segmentation_method': review['segmentation_method'],
                'semantic_coherence': review.get('semantic_coherence', 0),
                'detection_method': review.get('detection_method', 'unknown'),
                'detected_gender': detected_gender
            }
            for col in row.index:
                if col != text_column:
                    new_row[col] = row[col]
            results.append(new_row)
        return results
    except Exception as e:
        print_warning(f"Ошибка обработки строки {idx}: {e}")
        return []



def main():
    print("=" * 80)
    print("\033[1;35m🎯 ПРОФЕССИОНАЛЬНЫЙ СЕГМЕНТАТОР БАНКОВСКИХ ОТЗЫВОВ\033[0m")
    print("=" * 80)

    # Инициализируем профессиональный сегментатор
    try:
        segmenter = ProfessionalSegmenter()
        print_success("✅ Профессиональный сегментатор загружен")
        professional_mode = True
    except Exception as e:
        print_warning(f"⚠️ Не удалось загрузить профессиональный сегментатор: {e}")
        print_warning("Будет использована стандартная BERT сегментация")
        segmenter = None
        professional_mode = False

    # Загружаем BERT для определения сущностей
    bert_model = load_bert_model()
    if bert_model is None:
        print_error("Не удалось загрузить BERT модель. Проверьте установку sentence-transformers.")
        return

    print_success("✅ Модели готовы к работе")

    # Спросим пользователя, что он хочет сделать
    print("\n🔍 Выберите режим работы:")
    print("1 - Интерактивный анализ отзывов")
    print("2 - Пакетная обработка CSV файла")
    print("3 - Тестирование на примерах")
    print("4 - Сравнение сегментаторов")
    print("5 - Выход")

    try:
        choice = input("Введите номер варианта (1-5): ").strip()

        if choice == "1":
            print("\n🎯 Режим: ИНТЕРАКТИВНЫЙ АНАЛИЗ")
            if professional_mode:
                print("   ✅ Используется профессиональный сегментатор")
            else:
                print("   ⚠️ Используется стандартная BERT сегментация")
            interactive_bert_analysis(bert_model, segmenter)

        elif choice == "2":
            print("\n🎯 Режим: ПАКЕТНАЯ ОБРАБОТКА")
            if professional_mode:
                print("   ✅ Используется профессиональный сегментатор")
            else:
                print("   ⚠️ Используется стандартная BERT сегментация")

            # Настройки многопоточности
            print("\n⚡ НАСТРОЙКИ МНОГОПОТОЧНОЙ ОБРАБОТКИ:")
            print(f"   • Доступно ядер CPU: {multiprocessing.cpu_count()}")
            if professional_mode:
                print("   • Режим: Профессиональная сегментация")

            try:
                max_workers = int(input("   Введите количество потоков (рекомендуется 4-8): ") or "4")
                batch_size = int(input("   Введите размер батча (рекомендуется 20-100): ") or "50")
            except:
                max_workers = 4
                batch_size = 50
                print_warning("Использую значения по умолчанию: 4 потока, батч 50")

            input_file = input("Введите путь к CSV файлу: ").strip() or 'GaspromBank_dataset.csv'
            output_file = input("Введите путь для сохранения результатов: ").strip()

            if professional_mode:
                output_file = output_file or 'GaspromBank_professional_dataset.csv'
            else:
                output_file = output_file or 'GaspromBank_bert_parallel_dataset.csv'

            # Замер времени выполнения
            start_time = time.time()

            success = process_dataset_parallel(
                input_file,
                output_file,
                bert_model,
                segmenter=segmenter,  # Передаем сегментатор
                batch_size=batch_size,
                max_workers=max_workers,
                show_examples=False
            )

            total_time = time.time() - start_time

            if success:
                print("\n" + "=" * 80)
                print(f"\033[1;32m✅ ПАКЕТНАЯ ОБРАБОТКА УСПЕШНО ЗАВЕРШЕНА\033[0m")
                print(f"   ⏱️  Общее время выполнения: {total_time:.2f} секунд")
                print(
                    f"   🎯 Режим: {'Профессиональная сегментация' if professional_mode else 'Стандартная BERT сегментация'}")
                try:
                    df = pd.read_csv(input_file, sep=';', encoding='windows-1251', on_bad_lines='warn')
                    print(f"   🚀 Скорость обработки: {len(df) / total_time:.2f} отзывов/сек")
                except:
                    pass
                print("=" * 80)
            else:
                print_error("❌ ОБРАБОТКА ЗАВЕРШЕНА С ОШИБКАМИ")

        elif choice == "3":
            print("\n🎯 Режим: ТЕСТИРОВАНИЕ НА ПРИМЕРАХ")
            if professional_mode:
                print("   ✅ Используется профессиональный сегментатор")
                demonstrate_professional_analysis(segmenter, bert_model)
            else:
                print("   ⚠️ Используется стандартная BERT сегментация")
                demonstrate_bert_analysis(bert_model)

        elif choice == "4":
            print("\n🎯 Режим: СРАВНЕНИЕ СЕГМЕНТАТОРОВ")
            if professional_mode:
                compare_segmentators(segmenter, bert_model)
            else:
                print("❌ Профессиональный сегментатор недоступен для сравнения")
                print("Запустите тестирование на примерах (опция 3)")

        elif choice == "5":
            print("👋 Завершение работы...")
        else:
            print("❌ Неверный выбор")

    except KeyboardInterrupt:
        print("\n👋 Завершение работы по запросу пользователя...")
    except Exception as e:
        print_error(f"Ошибка: {e}")


def interactive_bert_analysis(bert_model, segmenter=None):
    """Интерактивный анализ с выбором сегментатора"""
    print_step("ИНТЕРАКТИВНЫЙ АНАЛИЗ ОТЗЫВОВ")

    if segmenter:
        print("✅ Доступен профессиональный сегментатор")
        use_professional = input("Использовать профессиональный сегментатор? (y/n): ").strip().lower()
        use_professional = use_professional in ['y', 'yes', 'да', 'д']
    else:
        use_professional = False
        print("⚠️ Профессиональный сегментатор недоступен, используется стандартный")

    while True:
        print("\n" + "=" * 70)
        text = input("📝 Введите отзыв для анализа (или 'выход'): ").strip()

        if text.lower() in ['выход', 'exit', 'quit']:
            break

        if not text:
            continue

        try:
            print("\n🔬 Анализируем текст...")

            if use_professional and segmenter:
                result = professional_bert_segmentation(text, segmenter, verbose=True)
                method = "Профессиональная сегментация"
            else:
                result = advanced_bert_segmentation(text, bert_model)
                method = "Стандартная BERT сегментация"

            print(f"✅ {method}: {len(result)} сегментов")

            print_entity_result(text, result)

            print(f"\n🔍 Детали анализа ({method}):")
            for i, item in enumerate(result, 1):
                method_icon = "🎯" if use_professional else "🤖"
                coherence = item.get('semantic_coherence', 0)
                confidence_icon = "🔴" if item['confidence'] == 'low' else "🟡" if item[
                                                                                     'confidence'] == 'medium' else "🟢"
                print(
                    f"   {i}. {method_icon} {confidence_icon} [{item['entity_type']}] (согласованность: {coherence:.2f})")
                print(f"      {item['text'][:80]}...")

        except Exception as e:
            print_error(f"Ошибка анализа: {e}")

        cont = input("\n▶️  Продолжить анализ? (y/n): ").strip().lower()
        if cont not in ['y', 'yes', 'да', 'д']:
            break


def demonstrate_professional_analysis(segmenter, bert_model):
    """Демонстрация работы профессионального сегментатора"""
    print_step("ДЕМОНСТРАЦИЯ ПРОФЕССИОНАЛЬНОЙ СЕГМЕНТАЦИИ")

    test_cases = [
        "Собираюсь в Армению, есть карта мир, хотела узнать работает ли она там, написала ВК 18.07 в 16:14, как обычно, ответили быстро, через 2 минуты. Сперва уточнила работают ли карты UP, ответили, что за границей нет, только в РФ. Далее спросила в каких странах принимают карты мир, и принимают ли в Армении, ответили про ограничения и про то, что в Армении можно использовать карту только в тех устройствах, где эквайер ВТБ. И уточнила ещё про визу, написали, что работает в РФ. Переписку прикрепляю. Благодарю за чёткие ответы",
        "Оформлял кредитную карту, менеджер был очень вежлив, все объяснил. Потом решил открыть вклад, но там уже другой сотрудник помогал, тоже все четко рассказал про проценты",
        "Мобильное приложение постоянно глючит, не могу войти в интернет-банк. Звонил в поддержку, долго ждал ответа, в итоге сказали обновить приложение, но не помогло"
    ]

    for i, text in enumerate(test_cases, 1):
        print(f"\n🧪 ТЕСТ {i}:")
        print(f"   📝 Текст: {text[:100]}...")

        # Профессиональная сегментация
        professional_result = professional_bert_segmentation(text, segmenter)
        print(f"   🎯 Профессиональная сегментация: {len(professional_result)} сегментов")

        for j, seg in enumerate(professional_result, 1):
            print(f"   ├── {j}. [{seg['entity_type']}] {seg['text'][:60]}...")

        # Стандартная сегментация для сравнения
        standard_result = advanced_bert_segmentation(text, bert_model)
        print(f"   🤖 Стандартная BERT сегментация: {len(standard_result)} сегментов")

        for j, seg in enumerate(standard_result, 1):
            print(f"   ├── {j}. [{seg['entity_type']}] {seg['text'][:60]}...")


def compare_segmentators(segmenter, bert_model):
    """Сравнение профессионального и стандартного сегментаторов"""
    print_step("СРАВНЕНИЕ СЕГМЕНТАТОРОВ")

    test_text = "Собираюсь в Армению, есть карта мир, хотела узнать работает ли она там, написала ВК 18.07 в 16:14, как обычно, ответили быстро, через 2 минуты. Сперва уточнила работают ли карты UP, ответили, что за границей нет, только в РФ. Далее спросила в каких странах принимают карты мир, и принимают ли в Армении, ответили про ограничения и про то, что в Армении можно использовать карту только в тех устройствах, где эквайер ВТБ. И уточнила ещё про визу, написали, что работает в РФ. Переписку прикрепляю. Благодарю за чёткие ответы"

    print("📊 Сравниваем сегментацию на проблемном примере:")
    print(f"📝 Текст: {test_text[:150]}...\n")

    # Замер времени
    start_time = time.time()
    professional_result = professional_bert_segmentation(test_text, segmenter)
    professional_time = time.time() - start_time

    start_time = time.time()
    standard_result = advanced_bert_segmentation(test_text, bert_model)
    standard_time = time.time() - start_time

    print(f"🎯 ПРОФЕССИОНАЛЬНЫЙ СЕГМЕНТАТОР:")
    print(f"   • Сегментов: {len(professional_result)}")
    print(f"   • Время: {professional_time:.3f} сек")
    for i, seg in enumerate(professional_result, 1):
        print(f"   {i}. [{seg['entity_type']}] {seg['text'][:70]}...")

    print(f"\n🤖 СТАНДАРТНЫЙ BERT СЕГМЕНТАТОР:")
    print(f"   • Сегментов: {len(standard_result)}")
    print(f"   • Время: {standard_time:.3f} сек")
    for i, seg in enumerate(standard_result, 1):
        print(f"   {i}. [{seg['entity_type']}] {seg['text'][:70]}...")

    print(
        f"\n📈 ВЫВОД: Профессиональный сегментатор {'быстрее' if professional_time < standard_time else 'медленнее'} и дает {'лучшие' if len(professional_result) < len(standard_result) else 'сравнимые'} результаты")


# Обновляем функцию обработки для поддержки сегментатора
def process_single_review(args):
    idx, row, text_column, bert_model, segmenter = args  # Добавляем segmenter
    try:
        text = str(row[text_column])
        if not text or text.lower() in ['nan', 'none', '']: return []
        detected_gender = detect_gender_from_text(text)

        # Используем профессиональный сегментатор если доступен
        if segmenter:
            split_reviews = professional_bert_segmentation(text, segmenter)
        else:
            with bert_lock:
                split_reviews = advanced_bert_segmentation(text, bert_model)

        results = []
        for review in split_reviews:
            new_row = {
                'original_review_id': idx,
                'text': review['text'],
                'entity_type': review['entity_type'],
                'service_channel': review['service_channel'],
                'combined_entity_name': review['combined_entity_name'],
                'entity_category': review['entity_category'],
                'is_entity_specific': review['is_entity_specific'],
                'has_service_context': review['has_service_context'],
                'confidence': review['confidence'],
                'segmentation_method': review['segmentation_method'],
                'semantic_coherence': review.get('semantic_coherence', 0),
                'detection_method': review.get('detection_method', 'unknown'),
                'detected_gender': detected_gender
            }
            for col in row.index:
                if col != text_column:
                    new_row[col] = row[col]
            results.append(new_row)
        return results
    except Exception as e:
        print_warning(f"Ошибка обработки строки {idx}: {e}")
        return []


if __name__ == "__main__":
    main()